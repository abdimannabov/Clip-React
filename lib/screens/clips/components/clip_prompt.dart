import 'package:flutter/material.dart';

class ClipPrompt extends StatelessWidget {
  const ClipPrompt({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Guess the movie', style: TextStyle(color: Colors.white));
  }
}
